# App
PROJECT_NAME=Notion Clone API
VERSION=1.0.0
API_V1_STR=/api/v1

# Database (PostgreSQL do Railway)
# Obtenha a URL do banco no Railway: Settings > Variables > DATABASE_URL
DATABASE_URL=postgresql://user:password@host:port/database

# Redis (opcional - descomente se tiver Redis configurado no Railway)
# REDIS_URL=redis://host:port

# JWT (gerar com: openssl rand -hex 32)
SECRET_KEY=sua-chave-secreta-super-segura-aqui
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS (adicione suas URLs de frontend)
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173","https://seu-frontend.railway.app"]

# Frontend URL (para links de convite, etc)
FRONTEND_URL=http://localhost:3000

# Email (opcional - para recuperação de senha)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASSWORD=sua-senha-app

# Cloudinary (opcional - para upload de arquivos)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
