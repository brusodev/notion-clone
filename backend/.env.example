# Database
# Desenvolvimento local (SQLite - sem instalação necessária!):
DATABASE_URL=sqlite:///./notion_clone.db

# Produção (Railway fornece automaticamente PostgreSQL):
# DATABASE_URL=postgresql://user:password@host:port/database

# Redis (opcional - a aplicação funciona sem Redis)
REDIS_URL=redis://localhost:6379

# JWT (gerar com: openssl rand -hex 32)
SECRET_KEY=sua-chave-secreta-super-segura-aqui
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS (adicionar URL do frontend quando deployar)
ALLOWED_ORIGINS=["http://localhost:3000","https://seu-frontend.railway.app"]
